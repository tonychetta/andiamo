import { PageStub } from "@/components/PageStub";

export default function ResultsPage() {
  return (
    <PageStub
      eyebrow="The proof"
      title="Results"
      blurb="Follower growth, monthly listeners, and stream counts over time — the proof that the daily work is compounding."
    />
  );
}
